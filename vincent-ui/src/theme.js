import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        fontFamily: 'Nunito, sans-serif', // Đảm bảo font chữ của bạn ở đây
    },
});

export default theme;
